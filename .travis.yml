language: node_js

node_js:
  - 8.9.3

sudo: required
services:
 - docker

before_script:
 - docker build -t studio-frontend --no-cache .

script:
  - npm run lint
  - npm run test

after_script:
  - codecov
